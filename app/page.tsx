import Typography from "@mui/material/Typography";

const Page = () => {
    return (
        <Typography>
            This application that enables us to create and store recipes
        </Typography>
    );
};

export default Page;
